const Builders = require("../models/BuilderModel");
const Users = require("../models/userModel");

const builderCtrl = {
    registerBuilder: async (req,res) =>{
        try {
            
        } 
        catch (error) {
            
        }
    }
}

module.exports = builderCtrl;